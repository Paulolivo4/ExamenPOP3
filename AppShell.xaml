<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      xmlns:views="clr-namespace:ExamenPOP3"
      x:Class="ExamenPOP3.AppShell">
    
    <ShellContent Title="Cocktails"
                 ContentTemplate="{DataTemplate views:CocktailListPage}" />
    <ShellContent Title="Saved Cocktails"
                 ContentTemplate="{DataTemplate views:SavedCocktailsPage}" />
    
</Shell>